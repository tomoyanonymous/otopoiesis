
`{
let phasor = | |{(self+1.0)% 44100}
let dsp:()->float = Project!("test project ",`| | {
        let res = sin(440*3.1415 * 2*phasor() /44100)
        let gain = Slider!("gain",0.2,0.0,1.0)
        0
    })
}